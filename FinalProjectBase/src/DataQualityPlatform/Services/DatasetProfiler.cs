using DataQualityPlatform.Models;

namespace DataQualityPlatform.Services;

public class DatasetProfiler
{
    public DatasetProfile Profile(Dataset dataset)
    {
        // TODO(STUDENT): Calculate row count and column count.
        // TODO(STUDENT): Detect the data type for each column.
        // TODO(STUDENT): Calculate missing value count and percentage for each column.
        // TODO(STUDENT): Calculate unique value count for each column.
        // TODO(STUDENT): Calculate minimum, maximum, and mean for numeric columns.
        throw new NotImplementedException("TODO: Student implementation.");
    }
}
